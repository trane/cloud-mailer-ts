# Tests gRPC cloud-mailer client

## NOTE

This _requires_ Node 0.10.x or greater.

## USAGE

You must have a `CLOCKTOWER_ROOT` envar set, otherwise nothing will build/work.

```sh
$ npm run start:static
```
